# Quickstart

1. Pick your binary in `bin/<platform>/`.
2. Get credentials from `${BRIDGE_SERVER_URL}/user/login`.
3. Configure auth via env vars or `examples/bridge-auth.json.example`.
4. Start bridge and point your app to the virtual serial endpoint.
